name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  test-jobs:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls -lah
  test-jobs2:
    runs-on: self-hosted
    steps:
      - run: pwd
      - run: ls -lah
